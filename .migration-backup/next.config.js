/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable CSS Modules (built-in)
  // Add image domains as needed
  images: {
    remotePatterns: [],
  },
};

module.exports = nextConfig;
